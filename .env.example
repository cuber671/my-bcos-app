# 环境变量配置示例
# 复制此文件为 .env 并填写实际值
# .env 文件已添加到 .gitignore，不会被提交到Git
#
# 注意：数据库用户名和密码已在 application.properties 中配置
# 此文件主要用于配置 JWT_SECRET

# ========== JWT密钥配置（必需） ==========
# 必须使用至少256位（32字节）的强随机密钥
# 生成命令: openssl rand -base64 64
JWT_SECRET=your-very-secure-random-jwt-secret-key-at-least-256-bits-long-change-this-in-production

# ========== 生产环境配置说明 ==========
# 生产环境部署时：
# 1. 修改 application.properties 中的数据库连接信息
# 2. 在此文件中设置生产环境的 JWT_SECRET
# 3. 确保使用强密码保护数据库

